# Changelog

本文件记录项目每个版本的重要变化。

## [Unreleased]

### Added

- 增加“材料收件箱”：支持一次粘贴杂乱文字、HTML、截图、PDF、文本及其他文件，再由共用全局 Provider 的视觉模型拆分为可编辑材料；本地模式提供明确标注的保守规则分类。
- PDF 材料会在本机提取最多 20 页文字，并渲染前 3 页作为视觉模型预览，使扫描版 PDF 也能进入结构提取流程。
- 增加持久化三类素材银行：CV、职位描述和个人资料。每次确认入库同时保存原始粘贴与附件，结构化条目可按分类查看、复用或从列表移除。
- 增加从素材生成 CV 的完整流程：整合 CV 与个人资料，可选目标职位关键词，使用 MIT 许可的 `geekplux/cv_resume` portable edition 生成可离线编译的独立 LaTeX 项目并自动加入“我的 CV”。
- 增加持久化“我的 CV”项目库，可管理多个独立本地 LaTeX 简历文件夹，并支持导入、切换和仅从列表移除。
- 支持把当前 CV 复制为新的独立项目：保留嵌套源码与二进制资源、当前未保存内容和所选主文档，同时排除 Git 数据、回收区、忽略目录与编译产物；每个项目继续使用独立 Agent 对话。
- 增加 PDF 交互审阅 MVP：可从预览进入全屏圈选模式，在渲染页上自由圈画或用键盘选择整页，再把圈选区域直接交给 Resume Agent。
- PDF 圈选上下文同时包含本机提取的区域文字与带标记的图像裁剪；视觉模型据此理解版式，并通过受限搜索工具定位真实的多文件 LaTeX 源码。
- 增加全局 AI Provider 设置，Resume Agent 与 PDF 视觉 Agent 共用同一组 Provider、Model、API key、Base URL 和修改审批偏好。
- Resume Agent 文件工具增加明确的 `create` / `update` / `delete` 操作；删除会参与临时编译验证，并在界面中显示完整移除 diff。

### Security

- 材料收件箱仅在用户点击“分析并整理”后向已配置的远程 Provider 发送当次文字与受支持的图片；入库前必须人工确认。附件限制为单个 5 MB、总计 16 MB，非安全内联类型下载时强制使用二进制 MIME。
- 图片与 PDF 只作为来源凭证保存；客户端与服务端都会拒绝没有提取文字或结构化字段的条目，避免把不透明文件误当作可复用知识。
- PDF 视觉上下文仅在用户主动发送远程 Agent 请求时传输；服务端限制图像类型、区域坐标和 2.5 MB 大小，本地检查模式不会上传图像。
- Agent 不能删除当前 LaTeX 主文档；所有删除都忽略自动应用设置、要求手动确认，并移动到项目内 `.cvstudio-trash/`。应用前继续校验路径、文件存在状态和内容 hash，拒绝过期提案。

### Changed

- 信息银行列切换改为整列从中心沿水平方向平顺拉伸铺开，左右边缘同步移动、无回弹，并在框体稳定后显示内容，不再呈现从单侧或角落长出的感觉。
- 根据交互反馈撤回模糊与毛玻璃过渡，恢复最初版的轻量位移和淡入淡出动效，同时继续遵守 `prefers-reduced-motion`。
- 信息银行移除 CV 分类，改为“全部 / 职位描述 / 个人信息”三列伸缩视图；点击列后该列平滑扩展为主内容区，其余列收起，同时遵守减少动态效果设置。
- CV 现在只作为解析来源，不再整份入库；模型与本地规则会拆出个人简介、工作经历、项目经历、教育经历、专业技能、荣誉和奖项、课外活动、社会实践、论文发表、演讲和讲座等独立信息，并按常用程度排序展示。
- 重组左侧导航：把带图标的“材料库”置于功能列表顶部，提供独立的“收件箱 / 信息银行”双入口；“我的简历”与编辑器归入“简历工作流”，信息银行明确展示每条内容的当前状态与录入日期，方便区分和管理版本。
- 将 Resume Agent、Hermes Gateway 与材料分析的远程请求超时从 120 秒提高到 300 秒，长任务不再过早中止。
- 素材银行改为按 CV / 职位 / 个人资料类型展示 profile、经历、项目、教育、技能、职位要求和关键词；来源附件缩为次要凭证。生成入口明确创建新的 CV 并保留已有版本。

- 重新设计完整 macOS 界面为暖色纸张、深色墨水和橙色强调的工作台视觉，移除蓝紫渐变、玻璃拟态与通用 AI Dashboard 观感。
- 按钮、导航、文件树、窗格、Resume Agent、模拟面试和对话框统一使用清晰硬边框、像素细节与可按压的偏移阴影，同时保留键盘焦点和 `prefers-reduced-motion`。
- 首页功能场景与深色模式改为更具辨识度的本地创作工具风格；所有字体、图案与图标继续随 App 本地打包。
- 收敛像素风格为品牌标记、状态方块与 Agent 头像等少量细节；主体改为更平、更轻的 Raw Minimalism，以细边框、留白和内容层级替代装饰性点阵与大面积偏移阴影。
- 橙色调整为低饱和浅陶土色，减少首页、按钮、导航和深色模式中的强烈色彩对比。
- 在 Raw Minimalism 骨架上恢复克制的连续圆角、半透明材质和柔和层级阴影；毛玻璃只用于顶栏、展示卡、主要窗格、浮动 Agent 与对话框，避免让高密度内容失去清晰度。
- 使用本地打包的 Mozilla PDF.js 替换 Chromium 内置 PDF 插件，提供统一的分页、缩放、适宽/整页、旋转和下载工具栏，并为后续搜索、标注和源码定位保留应用内扩展层。
- 修正编辑器顶栏“打开 / 专注 / 保存 / 编译”按钮中图标与文字的垂直对齐。

### Fixed

- Agent 等待回复时改用固定尺寸、仅改变透明度的进度指示器，并移除消息入场缩放，避免消息气泡在等待动画期间改变高度。
- 全局 `1` / `2` 页面快捷键现在只响应无修饰键且焦点不在输入区域时的按键，避免 `Shift+2` 或编辑文字时误跳到“模拟面试”。

## [0.10.0] - 2026-08-07

### Added

- 引入本地打包的 Motion 与 Lucide，统一视图切换、Agent 消息、悬浮窗、主题切换和界面图标。
- macOS App 使用原生 Finder 目录选择器打开 LaTeX 项目，网页运行时继续保留路径输入对话框。

### Changed

- 全面重构为 macOS 视觉系统：系统字体、半透明材质、原生蓝色强调色、连续圆角、柔和层级阴影和紧凑工具栏。
- 移除像素按钮、阶梯阴影和硬边框造成的视觉冲突，重新设计首页场景、侧栏、工作区、Agent、面试面板与对话框。
- 所有新增动画继续遵守 `prefers-reduced-motion`。

## [0.9.0] - 2026-08-07

### Added

- 增加 Electron macOS App 主进程、原生应用菜单、安全窗口配置和动态本机端口。
- 默认项目首次启动时复制到 macOS Application Support，后续启动保留用户内容。
- 增加 Apple Silicon DMG/ZIP 打包配置与 ad-hoc 签名；Tectonic 二进制与缓存作为可执行外部资源随 App 分发。

### Changed

- 本地服务支持通过环境变量配置应用资源、工作区与 Tectonic 路径，并可嵌入桌面进程启动和关闭。

## [0.8.2] - 2026-08-07

### Added

- 增加 Sites 云端发布入口：简历源码和编辑状态仅保存在访问者当前浏览器的 localStorage 中。
- 云端版提供内置多文件示例、本地结构检查、修改预览和模拟面试，不依赖远程字体或 CDN。

### Changed

- 云端环境明确禁用本机路径访问、Tectonic 编译和远程模型密钥配置；这些能力继续由桌面版提供。

## [0.8.1] - 2026-08-06

### Changed

- 收敛 pixel-art 视觉范围：只保留按钮反馈、状态点、Agent 头像、启动器和工具标签的像素细节。
- 移除全局网格背景、标题装饰块、粗分隔线、卡片硬边框、多层阶梯阴影与侧栏分组横线。
- 首页功能区改为无边框留白布局；悬浮 Agent 改回轻量中性浮层，降低对编辑器内容的遮挡感和视觉压迫。

## [0.8.0] - 2026-08-06

### Added

- 引入 AI SDK `ToolLoopAgent`，为 OpenAI 与 Anthropic 提供最多 8 步的真实项目 Agent；支持列文件、按需读取、搜索、确定性检查、内存提案与临时编译。
- 增加可选 Hermes Gateway Provider，支持其 OpenAI-compatible Responses / Chat Completions 接口，并在界面中明确提示更宽的本机权限边界。
- Agent 增加按项目的 session 会话历史、清空对话、模型状态和 `READ / SEARCH / PROPOSE / COMPILE` 工具轨迹。
- 增加 Agent 工具单元测试与 mock 模型端到端循环测试。

### Changed

- 远程 Provider 缺少 API key 时改为明确报错，删除静默回退成本地规则的误导行为；本地模式重命名为“本地检查（非 AI）”。
- Agent 修改只在内存中暂存；LaTeX 验证会复制完整项目与二进制资源到临时目录，编译后清理，不触碰用户当前文件。
- 全站建立自定义 pixel-art 设计 token，统一硬边框、阶梯阴影、状态像素、窗口、按钮、文件树与 Agent 反馈，同时保留正文和代码可读性。
- 删除 Google Fonts 与 Fontshare 网络字体导入，浏览器界面改为完全本地的系统字体栈。
- Agent 默认模型更新为 OpenAI `gpt-5.6-terra` 与 Anthropic `claude-sonnet-4-6`，并允许显式自定义 Base URL 和 API 模式。

### Security

- Agent 工具输入使用 Zod 校验；保留项目相对路径、文件大小、扩展名、内容 hash 与 patch 校验。
- Agent 不具备 shell、删除文件或直接写盘工具；文件内容明确作为不可信数据处理。

## [0.7.0] - 2026-08-06

### Added

- Agent 支持自定义 OpenAI / Anthropic Base URL，并继续将地址与密钥仅保存在当前浏览器。
- Agent 输入框支持 `Enter` 发送、`Shift + Enter` 换行。

### Changed

- 侧栏折叠态改为 12px 窄轨道并完整隐藏导航内容；把手取消按下位移，窄屏时隐藏无效的折叠控制。
- Agent 悬浮时位置选择器明确显示“悬浮”，同时升级窗口、消息气泡、设置面板和输入区视觉。

## [0.6.2] - 2026-08-06

### Changed

- 简化侧栏交互：删除左缘悬停唤出和移出自动收回，只保留边缘小把手的单击折叠/展开。

## [0.6.1] - 2026-08-06

### Changed

- 侧栏折叠控制缩小并移动到分隔边缘；收起后支持在屏幕最左缘短暂停留唤出，移开自动收回。
- Resume Agent 取消拖拽边缘自动吸附，只保留自由拖动与标题栏中的显式停靠选择。
- Agent 最小化启动器提升到固定顶层，并增加 `Cmd/Ctrl + Shift + A` 召回快捷键。

### Removed

- 删除 CV 改进导航、页面、浏览器逻辑和 `/api/cv-review` 接口。

## [0.6.0] - 2026-08-06

### Added

- 使用 CodeMirror 6 替换原生 textarea，提供 LaTeX 语法高亮、行号、搜索、括号处理、撤销栈和标准编辑快捷键。
- 支持直接打开本地 LaTeX 项目文件夹，读取嵌套章节、`.cls` / `.sty` / `.bib` 文件与字体等二进制资源。
- 自动识别并切换多个可编译主文档；编译器以活动项目目录为工作目录，正确解析相对路径资源。
- 文件列表升级为可折叠层级树，纯二进制资源目录默认收起。
- 增加专注模式：文件树、编辑器和 PDF 占满窗口，支持 `Cmd/Ctrl + Shift + F` 进入和 `Esc` 退出。
- 使用 interact.js 增强窗格和 Agent 的拖拽体验，加入惯性、边界限制、实时尺寸提示和 Windows 风格边缘吸附区域。

### Changed

- 浏览器代码通过 esbuild 本地打包，CodeMirror 与 interact.js 均随项目离线提供，不依赖 CDN。
- 增强按钮、对话框、窗格落位、功能切换与 Zen Mode 的过渡动画，并继续尊重 `prefers-reduced-motion`。

### Tested

- 使用 `Daniel_Zhao_EN_Resume_DataSci_AutoPipe` 复杂项目验证多入口、嵌套文件、Awesome CV 模板、Roboto 字体、中英文编译和 PDF 预览。

## [0.5.1] - 2026-08-06

### Added

- 项目文件树支持拖拽调整宽度，并用“文件名 + 所在目录”两行展示长路径。
- Resume Agent 默认以浮动窗口打开，支持拖拽、缩放、最小化，以及右下、右上、左下和整块左侧停靠。
- 远程 Agent 支持返回结构化文件修改提案，在界面中预览 diff 后应用，也可选择自动应用。
- Agent 文件写入增加路径、内容 hash 和 patch 校验，防止过期提案覆盖已变化的文件。

### Fixed

- 补齐 Agent 启动器、停靠菜单、固定按钮、最小化按钮、窗口拖拽缩放和文件树 splitter 的事件绑定。
- 增加结构化编辑提案的安全过滤与 patch 生成测试。

## [0.5.0] - 2026-08-06

### Added

- 页面切换增加短促的淡入 / 上移过渡，并尊重 `prefers-reduced-motion`。
- 编辑器升级为多文件 LaTeX 项目：文件树、文件切换、新建文件和整项目编译。
- 工作台改为三窗格：LaTeX 编辑器、PDF 预览、Resume Agent 交互窗口。
- 编辑器与右侧区域支持拖拽调整大小，每个 pane 都可以隐藏并恢复。
- Resume Agent 支持本地 fallback，以及用户自行配置的 OpenAI / Anthropic provider。
- 增加 [agent-research.md](docs/agent-research.md) 记录框架调研和集成边界。

### Changed

- 编译 API 从单一 `resume.tex` 扩展为项目文件快照，同时保留旧的 document API 兼容入口。
- 最低 Node.js 版本从 20 提升到 22，为后续使用维护中的 Agent SDK 保留兼容性。

## [0.4.1] - 2026-08-06

### Added

- 侧栏支持折叠，折叠状态保存在本机浏览器。
- 工作区导航增加层级：`简历工作流`包含简历编辑器和 CV 改进，`面试准备`包含模拟面试。

### Changed

- 折叠侧栏时保留功能图标、当前选中状态和 Tectonic 状态指示，减少工作区占用。

## [0.4.0] - 2026-08-06

### Added

- 项目内置 Tectonic 0.15.0 和离线资源缓存，默认简历可以不依赖 MacTeX 编译。
- 首屏产品首页，提供产品概览、隐私边界和三个主要入口。
- 左侧工作区导航与渲染器健康状态。
- 编辑器浅色 / 深色主题切换，主题选择保存在本机浏览器。

### Changed

- 工作区从顶部标签导航调整为左侧功能栏，适配桌面和移动端。

## [0.3.0] - 2026-08-05

### Changed

- 参考 `codex-pet-share` 的暖灰、近黑、橙色视觉变量重做工作台外观。
- 引入 Cabinet Grotesk、Clash Display 和 JetBrains Mono 字体层级，并加入轻微颗粒质感。
- 调整顶栏、导航、按钮、输入框、空状态和结果 surface 的间距、圆角与阴影。
- 保持编辑器、CV 分析和模拟面试的功能与本地数据边界不变。

## [0.2.0] - 2026-08-05

### Added

- 统一工作区导航：编辑器、CV 改进、模拟面试。
- 本地 CV 分析：结构检查、量化表达检测、行动动词检查和岗位关键词匹配。
- 本地模拟面试：行为、技术、产品三种方向，逐题回答、结构化评分和追问。
- 中英文回答长度估算，避免中文回答被空格分词误判。
- 温暖中性色视觉系统和移动端工作区布局。

## [0.1.0] - 2026-08-05

### Added

- 本地 LaTeX 源码编辑与持久化。
- 使用 `latexmk` 或 `pdflatex` 编译简历。
- 编译后的 PDF 即时预览。
- LaTeX 错误输出面板。
- 桌面与窄屏基础适配。
- 一份可直接修改的极简简历模板。
